services:
  postgres:
    image: postgres:16
    container_name: comments-db
    environment:
      POSTGRES_DB: commentsdb
      POSTGRES_USER: admin
      POSTGRES_PASSWORD: admin123
    ports:
      - "5432:5432"
